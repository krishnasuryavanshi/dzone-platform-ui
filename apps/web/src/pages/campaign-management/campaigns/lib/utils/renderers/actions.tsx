import { ICampaign } from '../../types';

// TODO: Import CampaignRowActions component when available
export const actionsRenderer = (_val: unknown, _record: ICampaign) => {
  // TODO: return <CampaignRowActions campaign={record} />;
  return null;
};
